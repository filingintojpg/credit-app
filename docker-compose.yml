version: '3.8'

services:
  postgres:
    image: postgres:18
    container_name: postgres-container
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "${DB_PORT}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 10

  application-service:
    build:
      context: ./application-service
      dockerfile: Dockerfile
    container_name: application-service
    ports:
      - "${APPLICATION_SERVICE_PORT}:8080"
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: ${DB_NAME}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      CAMUNDA_URL: http://decision-service:${DECISION_SERVICE_PORT}/engine-rest
      CAMUNDA_USERNAME: ${CAMUNDA_USERNAME}
      CAMUNDA_PASSWORD: ${CAMUNDA_PASSWORD}
    depends_on:
      postgres:
        condition: service_healthy
      decision-service:
        condition: service_started

  decision-service:
    build:
      context: ./decision-service
      dockerfile: Dockerfile
    container_name: decision-service
    ports:
      - "${DECISION_SERVICE_PORT}:8090"
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: ${DB_NAME}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
    depends_on:
      postgres:
        condition: service_healthy
